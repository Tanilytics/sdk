# videojs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build videojs` to build the library.

## Running unit tests

Run `nx test videojs` to execute the unit tests via [Vitest](https://vitest.dev/).
